Usage: wyd <command> [flags]

whatcha doing? a time tracking helper

Commands:
  config               view wyd configuration
    show               print config to stdout

  client               work with client list
    list (show,ls)     list all clients
    add (create,a)     add a new client
    remove (delete)    remove a client

Flags:
  -h, --help    Show context-sensitive help.

Run "wyd <command> --help" for more information on a command.

(*fmt.wrapError)(parsing args: expected one of "config", "client")
(int) 0
