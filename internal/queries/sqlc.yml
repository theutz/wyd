version: "2"
sql:
  - &base
    engine: "sqlite"
    queries: "clients.sql"
    schema: "../migrations"
    gen:
      go:
        package: "clients"
        out: "clients"
  - <<: *base
    queries: "projects.sql"
    gen:
      go:
        package: "projects"
        out: "projects"
