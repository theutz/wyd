version: "2"
sql:
  - &base
    engine: "sqlite"
    queries: "clients.sql"
    schema: "migrations"
    gen:
      go:
        package: "clients"
        out: "queries/clients"
  - <<: *base
    queries: "projects.sql"
    gen:
      go:
        package: "projects"
        out: "queries/projects"
