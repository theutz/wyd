version: "2"
sql:
  - &base
    engine: sqlite
    queries: "clients/query.sql"
    schema: "../db/migrations"
    gen:
      go:
        package: clients
        out: "clients"
  - <<: *base
    queries: "projects/query.sql"
    gen:
      go:
        package: projects
        out: "projects"
